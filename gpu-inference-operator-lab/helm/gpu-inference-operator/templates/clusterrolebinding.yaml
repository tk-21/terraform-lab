apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "gpu-inference-operator.fullname" . }}-manager-rolebinding
  labels:
    {{- include "gpu-inference-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "gpu-inference-operator.fullname" . }}-manager-role
subjects:
  - kind: ServiceAccount
    name: {{ include "gpu-inference-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
