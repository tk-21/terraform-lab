---
# CIS Benchmark ハンドラー

- name: restart sshd
  ansible.builtin.systemd:
    name: sshd
    state: restarted

- name: restart auditd
  ansible.builtin.systemd:
    name: auditd
    state: restarted
