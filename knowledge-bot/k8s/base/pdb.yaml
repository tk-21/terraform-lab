# ノード更新や退避時でも最低 1 Pod は残すようにする。
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: knowledgebot
  namespace: knowledgebot
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: knowledgebot
