# Cluster 内で Deployment の Pod を 80 番ポートで公開する。
apiVersion: v1
kind: Service
metadata:
  name: knowledgebot
  namespace: knowledgebot
spec:
  selector: { app: knowledgebot }
  ports:
    - port: 80
      targetPort: 8080
