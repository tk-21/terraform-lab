# Pod から AWS API を呼ぶため、IRSA ロールを紐づける ServiceAccount。
apiVersion: v1
kind: ServiceAccount
metadata:
  name: knowledgebot-sa
  namespace: knowledgebot
  annotations:
    eks.amazonaws.com/role-arn: REPLACE_WITH_IRSA_APP_ROLE_ARN
