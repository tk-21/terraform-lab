# アプリが参照する非機密の環境変数をまとめて渡す。
apiVersion: v1
kind: ConfigMap
metadata:
  name: knowledgebot-config
  namespace: knowledgebot
data:
  AWS_REGION: REPLACE_AWS_REGION
  RAG_MODE: REPLACE_RAG_MODE
  BEDROCK_MODEL_ID: REPLACE_BEDROCK_MODEL_ID
  KNOWLEDGE_BASE_ID: REPLACE_KNOWLEDGE_BASE_ID
