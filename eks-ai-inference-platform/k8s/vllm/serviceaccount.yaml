apiVersion: v1
kind: ServiceAccount
metadata:
  name: vllm-sa
  namespace: ai-inference
  annotations:
    # IRSAによるS3モデルキャッシュへのアクセス権付与
    # VLLM_IRSA_ROLE_ARN は `terraform output vllm_irsa_role_arn` で取得した値に置換すること
    eks.amazonaws.com/role-arn: "VLLM_IRSA_ROLE_ARN"
