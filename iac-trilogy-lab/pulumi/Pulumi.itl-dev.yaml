config:
  aws:region: ap-northeast-1
  # notificationEmail は pulumi config set --secret で設定すること:
  #   pulumi config set notificationEmail your@email.com --secret
  # awsAccountId は pulumi config set で設定すること:
  #   pulumi config set awsAccountId 123456789012
